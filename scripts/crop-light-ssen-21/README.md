# 라이트쎈 중2-1 (스캔본) — 898문항 업로드 완료 (2026-09-22)

파일 (`C:\Users\USER\문제은행\중2-1\중2-1\(개정) 라이트쎈\`)
- `22개정 라이트쎈 중2-1.pdf` (문제, 173쪽)
- `라이트쎈 2-1 정답.pdf` (정답 및 풀이, 120쪽) — **앞 7쪽이 「빠른 정답 찾기」 압축 목록**
- `라이트쎈 2-1 빠른정답.pdf` (7쪽, 글자층이 깨져 있어 안 씀)

## 중1-2와 다른 점 — 정답 연결이 훨씬 쉬웠다
중1-2는 해설 본문에서 「답」 표시를 찾아 번호를 맞춰야 했지만,
중2-1은 **정답 PDF 앞쪽의 「빠른 정답 찾기」 목록**(번호 + 답이 한 줄에 나란히)을 그대로 쓰면 된다.
- `adet.py`/`ascan.py` — 목록에서 번호 덩어리 1,182개 검출
- `aassign.py` — 문제집과 같은 본보기(`qcents.npy`)로 해독 + LIS 로 1,116개 확정
- `acrop.py` — 번호 오른쪽부터 **같은 줄의 다음 번호 앞**까지를 답 그림으로 자름

## 판 확인 (맨 먼저)
문제집 0013~0020 ↔ 정답 0013~0020 이 **8개 모두 일치** → 같은 판.
(중1-1은 여기서 어긋나서 작업을 접었다. 판 확인 없이 시작하지 말 것.)

## 순서
1. `qscan.py` → 번호 후보 1,414 + 묶음 지시문 203 (173쪽)
2. `qlab.py`(중1-2 본보기 재사용) → 해독, `qassign.py` → **문항 번호 1,128 / 1,172 (96%)**
3. `crop.py` → 문항 1,128개, 묶음 지시문 352개를 각 문항 위에 붙여서 자름
4. `adet/ascan/aassign/acrop.py` → 정답 1,116개
5. `pills3.py`(유형 머리말 173) · `concepts.py`(개념 머리말 69) → `pillsheet.py` 모아보기 5장을 **직접 읽어**
   `pillmap.py`·`conmap.py` 작성 (라이트쎈 개념장 06 = 학원 소단원 06+07 로 갈라진다)
6. `typed.py` → 읽기 순서로 직전 머리말의 유형을 붙임
7. `rows.py` → `scripts/problems_light_m21.json` + 잘린 이미지 폴더

## 꼭 알아야 할 함정 — 개념 상자가 한 쪽에 둘일 때
개념 상자는 **쪽 위쪽에 단을 가로질러** 놓이고 문항은 그 아래 2단으로 흐른다.
그래서 "읽기 순서로 직전 머리말" 규칙을 그대로 쓰면 **그 쪽의 A단계 문항이 전부 뒤 상자로 몰린다.**
해당하는 쪽이 6쪽(45문항) 뿐이라 그 쪽들을 직접 보고 경계를 박아 넣었다 — `typed.py` 의 `FIX`.
```
11:0054까지 01-12 · 44:0338까지 03-09 · 74:0536까지 05-13
93:0651까지 07-06 · 109:0741까지 08-15 · 125:0839까지 09-04
```

## 올린 것 / 뺀 것
- 올림 **898문항** (A단계 389 · B단계 509), 유형 153개, 레벨 A=1 · B=2, 전부 자기채점(정답 이미지)
- 뺀 것: 정답을 못 붙인 45문항, **학교시험 185문항**(유형 머리말이 없어 직전 유형을 물려받으므로 부정확)
- 검증: 정답 12쌍 눈으로 대조 12/12 정확, 유형 표본 10개 중 9개 바로 맞고 1개(0051)에서 위 함정을 찾아 고침
