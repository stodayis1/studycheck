# 라이트쎈 중2-2 — 748문항 업로드 완료 (2026-09-22)

파일 (`C:\Users\USER\문제은행\중2-2\중2-2\(개정) 라이트쎈\`)
- 문제집 `2022개정 중2-2 수학 라이트쎈.pdf` (스캔, 186쪽, 문항 1102개)
- 정답 **`[22개정]답지_라이트쎈중등수학2-2.pdf` (정답 및 풀이, 128쪽)** ← 이게 맞는 판이다

## ★ 안 맞는 정답 파일이 두 개 더 있다 — 쓰지 말 것
`라이트쎈 중2-2 빠른정답.pdf` 와 `[정답]+219+라이트쎈중등수학2(하)-pages-10-69.pdf` 는
**서로 같은 판이고, 문제집과는 다른 판이다.** 번호는 거의 같은데 문항이 군데군데 바뀌어 있다.

| 번호 | 문제집(=맞는 답지) | 안 맞는 파일 |
|---|---|---|
| 0046 | 5 cm | ① |
| 0299 | 14 cm | 50° |
| 0426 | 60° | 4 cm |

### 여기서 크게 속을 뻔했다 (꼭 읽을 것)
번호마다 단계 색(A 주황 · B 초록 · 학교시험 파랑)을 **전수 대조했더니 0001~0457 불일치 0건**이 나왔다.
"앞부분은 안전하니 그만큼만 올리자"고 판단했는데 **틀렸다.**
단계 대조는 *단계 경계를 넘는* 어긋남만 잡는다. 긴 학교시험 구간 안에서 문항이 바뀌면 경계가 도로 맞아 안 걸린다.
실제로 그 "안전하다던" 구간 안에 0299·0426이 있었다.
→ **판 확인은 반드시 '문제 내용 ↔ 정답 내용'으로, 구간마다 골고루 20문항쯤** 볼 것.

## 순서
1. `qscan.py` → 번호 후보 1320 + 묶음 지시문 188 (186쪽)
2. `qclus.py` → 글자 군집 → **`qauto.py` 로 자동 이름표**
   (다른 책 본보기로 대충 해독 → 확정된 번호로 클러스터에 다수결 이름표 → 재해독. 87% → 94%)
3. `qassign.py` → **문항 번호 1041 / 1101**
4. `crop.py` → 문항 1041개 (지시문 225개 붙임)
5. 정답: 해설집에서 뽑는다 (이 책은 빠른정답 목록이 없다)
   - `sdet.py` 해설 번호(색 글자) · `smark.py`+`sscan.py` **「답」 아이콘**(회색 문서 모양 26×26@200dpi) 1227개
   - `sglyph.py`+`snumassign.py` → 해설 번호 973개 확정(자기학습 반복)
   - `scrop.py` → 번호 다음에 오는 첫 「답」 아이콘 오른쪽을 잘라 정답 이미지 967개
6. `pills3.py`(유형 머리말 140) · `concepts.py`(개념 머리말 70) → 모아보기 5장을 직접 읽어
   `pillmap.py`·`conmap.py` 작성 → `typed.py`
7. `rows.py` → `scripts/problems_light_m22.json` + 잘린 이미지 폴더

## crop.py 에서 고친 것 (중2-1 판과 다름)
- **2단 고정**: 묶음 지시문 아래 번호가 들여쓰기 되므로 x로 단을 나누면 문제가 잘린다
- **같은 줄에 나란한 문항**(0310·0311 처럼)은 그 사이에서 자른다
- 묶음 지시문 후보는 **그 쪽 절반(단) 전체**에서 찾는다. 나란한 문항은 cx0 가 좁아져 있어서
  cx0 기준으로 찾으면 오른쪽 문항(0232 등)이 지시문을 못 받는다 ← 실제로 겪은 버그
- 지시문을 붙일 범위는 `bend.py` 로 **뒤 번호만 해독**해 정하고, 못 읽으면 다음 머리말 전까지(최대 8문항)

## 개념 상자가 한 쪽에 둘인 10쪽
`confix.py` 에 문항 구간별 유형을 박아 넣었다. **131쪽은 문항 순서가 개념 순서와 거꾸로다**
(0770 이 09-2, 0771~0775 가 09-1). 기계적으로 "직전 머리말"을 쓰면 틀린다.

## 올린 것 / 뺀 것
- 올림 **748문항** (A단계 271 = 레벨1 · B단계 477 = 레벨2), 유형 131개, 전부 자기채점(정답 이미지)
- 뺀 것: 정답을 못 붙인 75문항, 학교시험 218문항(유형 머리말이 없어 부정확)
- 검증: 정답 12쌍 눈으로 대조 12/12, 유형 표본 10개 10/10, A단계 지시문 표본 10개 확인
