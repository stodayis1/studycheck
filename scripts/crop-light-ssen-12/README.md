# 라이트쎈 중1-2 (스캔본) — 진행 상황 (2026-09-22)

파일: `[22년개정]라이트쎈 중1－2.pdf`(문제, 184쪽) · `라이트쎈 1-2 빠른정답.pdf`(실제로는 **정답 및 풀이** 88쪽)

## 판 확인 (중1-1 실패를 되풀이하지 않으려고 먼저 함)
- 문제집 0025~0036 ↔ 해설 0025~0036 답이 **12개 모두 일치** → **같은 판이다**

## 끝난 것
- 문항 번호 **1,036 / 1,085** (95%) 찾음 — `problem_numbers.json`
  - 번호 색이 단계마다 다르다(A=주황, B=초록, 학교시험=파랑) → 색으로 단계도 같이 구분
  - **일부 쪽은 흑백 스캔**이라 색으로 못 찾는다 → 흑백 예비 검출 필요(`qscan.py`)
  - 번호 해독은 숫자 덩어리를 모아 눈으로 이름표를 붙여(`qlab.py`) 본보기를 만든 뒤, **가장 긴 증가 수열(LIS)** 로 확정
- 문항 이미지 **1,036개** 자름 — 묶음 지시문([0025~0028])과 그림을 각 문항 위에 **붙여서** 자름(`crop.py`)
- 해설집의 "답" 표시 **1,193개** 검출(`answer_markers.json`, 템플릿 매칭)

## 남은 것 — 정답 연결
- 해설집 번호 해독이 약하다(고유 609/1,085). 간단정답 줄에서 164개, 표시-번호 짝짓기로 499개까지만 나옴
- 해설집 숫자 본보기를 더 정확히 만들거나(특히 붙어 있는 두 번호 분리), 단원별로 구간을 나눠 LIS를 돌리면 개선될 것

## 그 다음
- 유형: `유형 NN` 알약(검은 배경)을 찾아 모아보기로 읽으면 됨(중1-1에서 검증된 방법)
- 레벨(원장님 확인): A단계 1 · B단계 2 · 학교시험 3

## 2026-09-22 (이어서) — 정답 연결 성공
- 해설집 번호를 자동 인식만으로는 정확도가 모자랐다(확신 669/1,085, 표본 9개 중 1~2개 오답).
- 그래서 **답 표시 1,036개마다 그 항목의 번호 그림을 모아 16장으로 만들고 직접 읽었다**(`numsheet.py` → `answer_numbers_read.txt`).
  - 읽은 값 중 'x' 는 번호가 아닌 것(해설 본문 글자, 모의고사 구간)
- 결과 **정답 738문항** 연결 (`answer_map.json`, `acrop5.py` 로 잘라냄). 표본 9개 무작위 검사 **9/9 정확**
  - 예: 0031=9 cm, 0351=점 E, 0549=360°, 0929=① (원기둥 부피 108π → 구 한 개 36π), 0980=165 cm 이상 170 cm 미만

## 남은 것 — 유형 붙이기
- 유형 머리말(초록 '유형' 알약 + 검은 번호 동그라미 + 제목 + '개념 NN-N')을 찾는 중.
  색·크기가 배지(대표문제/B단계 표시)와 겹쳐서 아직 깔끔하게 안 걸러진다(`pills.py`, `pills2.py`)
- 유형을 못 붙이면 문제은행 화면에 안 보이므로 **업로드 보류**
- 다음 단계: 머리말만 정확히 검출 → 모아보기 3~4장으로 유형번호·제목 읽기 → 학원 중1-2 유형표(176개)와 제목으로 대응

## 2026-09-22 (마무리) — 유형까지 붙여 **645문항 업로드 완료**
- 유형 머리말: 초록 알약 **50×100픽셀**(유형 + 검은 번호 동그라미)로 찾으면 정확하다 → `pills3.py` (139개)
- A단계는 유형 머리말이 없다. 대신 **개념 머리말(빨간 알약 `01-3` 등)** 을 찾아(`concepts.py`, 75개) 개념→유형으로 붙였다
- 모아보기로 직접 읽어 대응표 작성: `pillmap.py`(유형 머리말→학원 코드), `conmap.py`(개념 머리말→학원 코드)
- 읽기 순서로 '직전 머리말'의 유형을 문항에 붙임 → 1,036문항 전부 유형 확보
- 검증: 0025→두 점 사이의 거리, 0400→두 삼각형이 합동이 되기 위해 추가로 필요한 조건, 0900→뿔대의 겉넓이 ✓

### 올린 것 / 뺀 것
- 올림 **645문항** (A단계 308 · B단계 337), 유형 106개, 레벨 A=1 · B=2, 전부 자기채점(정답 이미지)
- 뺀 것: 정답을 못 붙인 212문항, **학교시험 179문항**(유형 머리말이 없어 직전 유형을 물려받으므로 부정확 → 제외)
