# 라이트쎈 중3-2 — 376문항 업로드 (2026-09-22)

파일 (`C:\Users\USER\문제은행\중3-2\중3-2\라이트쎈\`)
- 문제집 `중3-2 수학 라이트 쎈 3-2.pdf` (스캔, 120쪽, 문항 645개)
- 정답 `정답_라이트쎈+중등수학3-2.pdf` (72쪽) — **앞 1~4쪽이 「빠른 정답 찾기」**
- 판 확인: 문제집 0012~0025 ↔ 정답 **14개 모두 일치**

## 이 책만의 어려움 — 스캔 채도가 낮다
번호·머리말이 색으로 인쇄돼 있는데 **스캔이 흐려서 글자 속이 회색**이다. 색만 보면 글자가 조각나서 못 찾는다.
→ `qdet.rednum`: **색이 있는 곳을 씨앗으로 부풀린 뒤(3×3, 2회) 어두운 화소와 겹치는 부분**을 글자로 본다.
   이 한 줄로 번호 확보가 68% → 89% 로 올랐다. `ascan.py` 의 정답 목록도 같은 방법(313 → 648).

## 순서
1. `qscan.py`(색+흑백, 흑백은 번호가 나오는 x 열 안에서만) → 후보 899 · 묶음 지시문 159
2. `qclus.py` → 군집 → `qlab.py`(모아보기 40칸을 눈으로 읽어 이름표) → `qself.py`
   - **번호는 모두 `0`으로 시작한다** → 첫 자리를 '0' 으로 고정 (첫 자리를 자주 틀렸다)
   - LIS + 자기학습 + 빈칸 메우기 → **문항 번호 575 / 645 (89%)**
3. `crop.py` → 문항 568개. **원본 번호와 `[0012~0017]` 을 흰색으로 덮는다**(아래 참고)
4. 정답: `ascan.py`→`adec.py`→`acrop.py` → **642 / 645 확정**, 조각 641개
5. 유형: `pills3.py`(유형 머리말 92) · `concepts.py`(개념 머리말 26) · `reps.py`(대표문제 배지 84)
6. `typed.py` → `rows.py` → `scripts/problems_light_m32.json`

## 유형 붙이기가 다른 책과 다르다
유형 머리말(초록 알약)이 **60%밖에 안 잡힌다.** 그래서 머리말만으로는 유형이 한 칸씩 밀린다.
→ **대표문제 배지를 세어** 유형 경계를 잡고(`reps.py`), 잡힌 머리말(`pillmap.py`)을 기준점으로 어긋남을 바로잡는다.
   유형 목록은 책 앞 **대단원 표지(6·42·90쪽)** 에 통째로 실려 있어 그대로 옮겼다 → `typemap.py`

## 품질 — 다른 책보다 낮다
표본 20개 중 2~3개에서 문항이 잘리거나 옆 문항이 섞여 들어갔다(스캔 품질 탓).
- `verify.py` 해독값 ≠ 배정값인 25개
- `audit.py` 번호를 지웠는데도 4자리 번호가 남은 57개(이 중 절반쯤은 헛짚음)
→ 둘을 합쳐 **82문항을 버리고** 376문항만 올렸다. 그래도 완벽하지는 않으니 이상한 게 보이면 이 목록(`drop.json`)을 손보면 된다.

## 원본 문항 번호 지우기 (이 책부터 적용)
학습지는 1, 2, 3… 으로 번호를 다시 매겨 인쇄하는데 그림에 교재 번호가 같이 들어가 학생이 헷갈렸다.
`crop.py` 에서
- 문항 번호 상자(`d.x0~x1`, `d.y0~y1`)를 흰색으로 덮고
- 묶음 지시문의 `[0012~0017]` 도 (`bb.x0~bx1`, `by0~by1`) 덮는다 — 그래서 `qscan.py` 가 지시문의 **오른쪽 끝(bx1)** 도 저장한다

대신 **교재·쪽·번호는 앱에서 본다**: `page_no`(교재에 인쇄된 쪽 = PDF 쪽 + 1)를 함께 올리고,
학습지 인쇄 정답표·QR 채점·채점 결과의 「출처」에 `라이트쎈 중3-2 54쪽 0303번` 처럼 나온다(`lib/problemSource.ts`).

## 올린 것 / 뺀 것
- 올림 **376문항** (A단계 166 = 레벨1 · B단계 210 = 레벨2), 유형 72개, 전부 자기채점
- 뺀 것: 자르기 불량 82, 학교시험 105, 이미지/정답 없음 12
- 검증: 정답 10쌍 눈으로 대조 10/10, 유형 표본 10개 중 8~9개 정확
